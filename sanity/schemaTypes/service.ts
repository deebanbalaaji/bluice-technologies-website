import { defineField, defineType } from "sanity";
export const service = defineType({ name: "service", title: "Service", type: "document", fields: [defineField({ name: "name", type: "string", validation: (rule) => rule.required() }), defineField({ name: "slug", type: "slug", options: { source: "name" } }), defineField({ name: "summary", type: "text" }), defineField({ name: "outcomes", type: "array", of: [{ type: "string" }] }), defineField({ name: "deliverables", type: "array", of: [{ type: "string" }] }), defineField({ name: "stage", title: "Best-fit stage", type: "text" })] });
